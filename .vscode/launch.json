{
    "configurations": [
        {
            "preLaunchTask": "debug",
            "type": "gdb",
            "request": "attach",
            "name": "Attach to gdbserver",
            "executable": "",
            "target": ":1234",
            "remote": true,
            "cwd": "${workspaceRoot}",
            "valuesFormatting": "parseText"
        }
    ]
}